# SceneBoard Gemini Instructions

Read and follow `AGENTS.md` before starting work.

For every task:

1. Read `rules/CRITICAL.md`.
2. Read `rules/RULES.md` and load only the branches relevant to the task.
3. For each target file, prefer the nearest nested `rules/RULES.md` when one exists.
4. If a task spans multiple packages, resolve the applicable rules for each package independently.

Do not duplicate or reinterpret the rules in this file. `AGENTS.md` and the routed `rules/` tree are the source of truth.
